{
  "name": "ios-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run copy-client && cap sync",
    "copy-client": "node copy-client.js",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
